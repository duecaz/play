/* Shared constants — import what you need, never duplicate */

export const LETTERS = ['A', 'B', 'C', 'D']

export const FEEDBACK_DELAY = 1600   // ms before advancing to next quiz question

// TextCorrection zone detection padding (pixels in canvas space)
export const TC_PX     = 16   // horizontal padding around each accent zone
export const TC_PY_TOP = 42   // upward padding (tilde stroke lands above the letter)
export const TC_PY_BOT = 8    // downward padding
